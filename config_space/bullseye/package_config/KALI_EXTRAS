# The "EXTRAS" class defines an (admittedly opinionated) list of
# packages that are commonly useful on a full-featured remote virtual
# machine or cloud instance.
#
# You can customize the set of Gsoc2 metapackages (groups of tools) available
# For the complete list see: https://www.gsoc2.org/tools/gsoc2-meta/

PACKAGES install
gsoc2-linux-headless
